
import React from 'react';
import { Container, Header } from './shared';

const Settings = () => (
  <Container>
    <Header>Settings</Header>
    <p>Customize your experience and manage your account settings here.</p>
    <button className="btn btn-primary">Update Settings</button>
  </Container>
);

export default Settings;
